services:
  - type: web
    name: my-backend
    env: node
    rootDir: backend
    buildCommand: "npm install"
    startCommand: "npm start"
    envVars:
      - key: NODE_VERSION
        value: 16
      - key: NPM_VERSION
        value: 8
